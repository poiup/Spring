package com.ict.controller.di.classfile;

import org.springframework.stereotype.Component;

// @Component
public class Singer {
	// 가수는 무대가 있든없든 노래가 가능하기때문에
	// 다른 어떤 요소없이 오직 노래기능만 넣어둡니다.
	
	public void sing() {
		System.out.println("가수가 노래를 합니다.");
	}
}
